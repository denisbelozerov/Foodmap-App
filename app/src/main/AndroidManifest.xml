<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodmap">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Foodmap">
        <activity
            android:name=".SelectedProducts"
            android:label="@string/selected_products"
            android:exported="false"
            android:theme="@style/NoActionBarTheme"/>
        <activity
            android:name=".SelectedRecipies"
            android:label="@string/selected_recipies"
            android:exported="false"
            android:theme="@style/NoActionBarTheme"/>
        <activity
            android:name=".RecipiesActivity"
            android:label="@string/all_recipies"
            android:exported="false"
            android:theme="@style/NoActionBarTheme"/>
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/all_products"
            android:exported="true"
            android:theme="@style/NoActionBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>